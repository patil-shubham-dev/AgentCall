package com.agentcall.app.ui.theme

import androidx.compose.ui.unit.dp

// ── Spacing grid (4dp base) ──────────────────
// Screen padding 20dp, section gap 24dp, card padding 16dp, grid gaps 12dp.

object Spacing {
    val XS = 4.dp
    val S = 8.dp
    val M = 12.dp
    val L = 16.dp
    val XL = 20.dp
    val XXL = 24.dp

    val ScreenPadding = XL
    val CardPadding = L
    val SectionGap = XXL
    val GridGap = M
}
