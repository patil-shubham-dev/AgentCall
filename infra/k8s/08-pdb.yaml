apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: voicebridge-backend
  namespace: voicebridge
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: voicebridge
      component: backend
